{% extends 'base.html.twig' %}

{% block title %}PublicMessage{% endblock %}

{% block body %}
    <h1>PublicMessage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ public_message.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ public_message.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('public_message_index') }}">back to list</a>

    <a href="{{ path('public_message_edit', {'id': public_message.id}) }}">edit</a>

    {{ include('public_message/_delete_form.html.twig') }}
{% endblock %}
